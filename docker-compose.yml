version: '3.8'

services:
  mongodb:
    image: mongo:latest
    ports:
      - 27017:27017
    environment:
      # - MONGO_INITDB_ROOT_USERNAME=mubarakdamteh10
      # - MONGO_INITDB_ROOT_PASSWORD=Mubarak12345
      - MONGO_INITDB_DATABASE=coin